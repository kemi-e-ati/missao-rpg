export function aleatorio(Lista){
    const posicao = Math.floor(Math.random()* Lista.length);
    return Lista[posicao];
}

const nomes = [Fabiana, Lucas, Pedro, Annunak, Fulana, Zé, Eeridug];
export const nome = aleatorio[nome];